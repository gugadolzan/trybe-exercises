# Crie e rode de modo interativo em modo 'Cleanup', a imagem andrius/ascii-patrol
docker run -it --rm andrius/ascii-patrol
# O -it permite a interação com o jogo pelo terminal e o --rm garante que o container será removido ao terminar o jogo

# Encerre o container utilizando os botões Ctrl+C
