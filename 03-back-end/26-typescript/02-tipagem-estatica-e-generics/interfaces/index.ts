import Pizza from "./Pizza";

const calabresa: Pizza = {
  flavor: "Calabresa",
  slices: 8,
};

const margherita: Pizza = {
  flavor: "Margherita",
  slices: 6,
};

const nutella: Pizza = {
  flavor: "Nutella",
  slices: 4,
};
